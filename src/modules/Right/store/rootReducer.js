import { combineReducers } from "redux";
// import { routerReducer as router } from "react-router-redux";

import rightData from "./app/reducer";

const rootReducer = combineReducers({
  rightData
  //   router
});

export default rootReducer;
